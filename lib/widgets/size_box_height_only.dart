import 'package:yc_wallet/share/quick_import.dart';

class SizedBoxHeightOnly extends SizedBox {
  const SizedBoxHeightOnly(double height, {Key? key})
      : super(key: key, height: height);
}

class SizedBoxWidthOnly extends SizedBox {
  const SizedBoxWidthOnly(double width, {Key? key})
      : super(key: key, width: width);
}
