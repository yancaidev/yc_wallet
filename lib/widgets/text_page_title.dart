import 'package:yc_wallet/share/quick_import.dart';

class TextPageTitle extends Text {
  const TextPageTitle(String data, {Key? key})
      : super(data,
            style: const TextStyle(fontWeight: FontWeight.bold, fontSize: 24),
            key: key);
}
